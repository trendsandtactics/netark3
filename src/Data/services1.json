"use client";
import React from "react";
import services from "../Data/services1.json";

const ServiceGrid = () => {
  return (
    <section className="py-20 bg-gray-50">
      <div className="max-w-7xl mx-auto px-6">
        {/* 3×3 GRID */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
          {services.map((service, i) => (
            <div
              key={i}
              className="flex flex-col justify-between bg-white rounded-xl shadow-md hover:shadow-lg transition-all duration-300 h-full p-8 text-left"
            >
              {/* ICON + TITLE */}
              <div>
                <img
                  src={service.icon}
                  alt={service.title}
                  className="w-14 h-14 mb-6 object-contain mx-auto lg:mx-0"
                />
                <h3 className="text-xl font-semibold text-gray-900 mb-4 text-center lg:text-left">
                  {service.title}
                </h3>

                {/* BULLET POINTS */}
                <ul className="list-disc list-inside text-gray-700 space-y-1 leading-relaxed text-sm lg:text-base">
                  {service.desc.map((point: string, index: number) => (
                    <li key={index}>{point}</li>
                  ))}
                </ul>
              </div>

              {/* BUTTON */}
              <a
                href={service.btnLink}
                className="mt-6 inline-block text-teal-600 font-semibold hover:underline text-sm lg:text-base"
              >
                {service.btnText} →
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};

export default ServiceGrid;
